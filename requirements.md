# やること管理アプリ 要件定義書

## 1. 本書の目的

本書は、やること管理アプリが満たすべき要件を定めるものである。

要件とは、「このアプリで何ができなければならないか」という条件を指す。本書では、データベースの具体的な構造と画面の詳しい配置は定めない。これらは、別途作成するDB設計書と画面設計書で定める。

本書は `handout_app_spec.md` をもとに作成している。

## 2. アプリの概要

### 2.1 目的

利用者が、自分のやることを登録して管理できるようにする。

### 2.2 利用者

- 利用者は1人とする。
- ログイン機能は設けない。
- 複数人でデータを共有する機能は設けない。

### 2.3 提供する機能

| 要件ID | 機能 | 概要 |
|---|---|---|
| FR-01 | 一覧 | 登録したやることを1画面に表示する |
| FR-02 | 登録 | 新しいやることを1件登録する |
| FR-03 | 編集 | 登録済みのやることを変更する |
| FR-04 | 削除 | 不要になったやることを削除する |
| FR-05 | 検索 | やることの名前やジャンルで一覧を絞り込む |
| FR-06 | 並び替え | 期限の順に一覧を並び替える |

要件IDとは、要件を見分けやすくするために付けた番号である。

## 3. 対象となる画面とURL

本アプリでは、やることを管理する6画面と、既存のトップページ1画面を使用する。URLとは、ブラウザで画面を開くためのアドレスを指す。

| 要件ID | 画面 | URL | 目的 |
|---|---|---|---|
| SC-01 | トップページ | `/` | 既存のトップページを表示する |
| SC-02 | 一覧 | `/todos` | 登録したやることを表示し、各操作の入口とする |
| SC-03 | 登録 | `/todos/new` | 新しいやることを入力する |
| SC-04 | 登録の確認 | `/todos/confirm` | 登録前に入力内容を確認する |
| SC-05 | 編集 | `/todos/{id}/edit` | 登録済みのやることを変更する |
| SC-06 | 編集の確認 | `/todos/{id}/confirm` | 保存前に変更内容を確認する |
| SC-07 | 削除確認 | `/todos/{id}/delete` | 削除前に対象を確認する |

`{id}` には、対象となる1件を見分ける番号が入る。

トップページは既存のものをそのまま使用する。やることを管理する6画面を新たな対象とする。

## 4. 機能要件

機能要件とは、アプリが利用者に提供する具体的な働きを指す。

### 4.1 一覧機能

一覧画面では、次の項目を左から順に表示する。

1. やること
2. ジャンル
3. 優先度
4. 期限
5. 状態
6. 操作

操作には、各行の「編集」と「削除」を表示する。

一覧画面の上部には、次の操作を配置する。

- やることの名前で絞り込む入力欄
- ジャンルで絞り込む選択欄
- 「絞り込む」ボタン
- 「期限が近い順」リンク
- 「期限が遠い順」リンク
- 「新規登録」ボタン

ジャンルの選択欄で「すべて」を選んだ場合は、ジャンルによる絞り込みを行わない。

保存されている優先度と状態は、次の表示に変換する。

| 保存されている値 | 画面に表示する言葉 |
|---|---|
| 優先度 `1` | 高 |
| 優先度 `2` | 中 |
| 優先度 `3` | 低 |
| 完了状態 `1` | 完了 |
| 完了状態 `0` | 未完了 |

表示するデータが0件の場合は、「まだ1件も登録されていません」と表示する。検索結果が0件の場合も同じ文を使用する。

一覧画面の最上部には、直前に行った操作の結果を1行で表示する。

### 4.2 登録機能

登録画面では、次の5項目を入力できるようにする。

| 項目 | 入力の要否 | 入力方法 | 初期状態 |
|---|---|---|---|
| やること | 必須 | 1行の文字入力 | 空欄 |
| メモ | 任意 | 1行の文字入力 | 空欄 |
| ジャンル | 必須 | 5つから選択 | 空欄 |
| 優先度 | 必須 | 高・中・低から選択 | 空欄 |
| 期限 | 任意 | 日付入力 | 空欄 |

「確認する」を選ぶと登録の確認画面へ進む。「一覧へ」を選ぶと一覧画面へ戻る。

### 4.3 登録内容の確認機能

登録の確認画面では、入力された5項目を入力欄ではなく文字として表示する。

入力されなかった任意項目には「（入力なし）」と表示する。

- 「登録する」を選ぶと、1件を登録して一覧画面へ移動する。
- 「書き直す」を選ぶと、入力済みの値を保ったまま登録画面へ戻る。

### 4.4 編集機能

編集画面では、登録画面の5項目に「完了」を加えた6項目を変更できるようにする。

画面を開いたときは、現在保存されている値を各項目に表示する。「完了」はチェックを入れる欄とする。

必須項目は、登録時と同じく次の3項目とする。

- やること
- ジャンル
- 優先度

「確認する」を選ぶと編集の確認画面へ進む。「一覧へ」を選ぶと一覧画面へ戻る。

### 4.5 編集内容の確認機能

編集の確認画面は、登録の確認画面と同じ形式で変更内容を文字として表示する。

- 「保存する」を選ぶと変更を保存して一覧画面へ移動する。
- 「書き直す」を選ぶと変更後の値を保ったまま編集画面へ戻る。

### 4.6 削除機能

削除確認画面では、削除する1件の次の項目を文字として表示する。入力欄は表示しない。

- やること
- ジャンル
- 期限

- 「消す」を選ぶと対象を削除して一覧画面へ移動する。
- 「やめる」を選ぶと削除せずに一覧画面へ戻る。

### 4.7 画面遷移

画面遷移とは、ボタンやリンクを押したときに別の画面へ移る流れを指す。

```text
一覧 ──「新規登録」──→ 登録 ──「確認する」──→ 登録の確認 ──「登録する」──→ 一覧
                          ↑                        │
                          └────「書き直す」────┘

一覧 ──行の「編集」──→ 編集 ──「確認する」──→ 編集の確認 ──「保存する」──→ 一覧
                          ↑                        │
                          └────「書き直す」────┘

一覧 ──行の「削除」──→ 削除確認 ──「消す」──→ 一覧
                            └──「やめる」──→ 一覧
```

一覧画面での絞り込みと並び替えは、一覧画面内で行う。

## 5. データ要件

データ要件とは、アプリが保存・管理する情報についての条件を指す。データはMySQLの `todos` テーブル1つに保存し、1件のやることを1行として扱う。

DBの型、主キー、既定値などの具体的な定義は、別途作成するDB設計書で扱う。

| 要件ID | データ項目 | 条件 |
|---|---|---|
| DT-01 | ID | 1件を見分ける番号。利用者は入力せず、自動で付ける |
| DT-02 | やること | 必須。255文字以内 |
| DT-03 | メモ | 任意。入力する場合は255文字以内 |
| DT-04 | ジャンル | 必須。決められた5種類から選ぶ |
| DT-05 | 優先度 | 必須。高・中・低をそれぞれ `1`・`2`・`3` として保存し、既定は中とする |
| DT-06 | 期限 | 任意の日付 |
| DT-07 | 完了状態 | 未完了または完了を保存し、既定は未完了とする |
| DT-08 | 登録日時 | 利用者は入力せず、MySQLが自動で記録する |
| DT-09 | 更新日時 | 利用者は入力せず、MySQLが自動で記録する |

ジャンルは、次の5種類に限定する。自由入力にはしない。

- デザイン
- マーケティング
- プログラミング
- 資格
- 就職活動

## 6. 入力チェック要件

入力チェックとは、入力された内容が決まりを守っているか確認する処理を指す。

決まりを満たさない場合は保存せず、入力画面に留まり、該当する欄の下にメッセージを表示する。

| 要件ID | 対象 | エラーとなる条件 | 表示するメッセージ |
|---|---|---|---|
| VL-01 | やること | 空欄 | やることを入力してください |
| VL-02 | やること | 255文字を超えている | やることは255文字以内で入力してください |
| VL-03 | メモ | 255文字を超えている | メモは255文字以内で入力してください |
| VL-04 | ジャンル | 選択されていない | ジャンルを選んでください |
| VL-05 | 優先度 | 選択されていない | 優先度を選んでください |

半角空白または全角空白だけが入力された場合も、空欄として扱う。

期限には入力内容に関する追加の決まりを設けない。

## 7. 検索・並び替え要件

### 7.1 検索

- 名前での絞り込みは、入力された文字が `title` の一部に含まれるデータを対象とする。
- ジャンルでの絞り込みは、選択された値と `category` が完全に一致するデータを対象とする。
- 名前とジャンルの両方が空の場合は、すべてのデータを表示する。
- 絞り込み結果が0件の場合は、「まだ1件も登録されていません」と表示する。

### 7.2 並び替え

- `due_date` の順に並び替える。
- 一覧を最初に開いたときは、期限が近い順に表示する。
- 利用者は「期限が近い順」と「期限が遠い順」を選択できる。

### 7.3 URLへの条件の保持

検索条件と並び順は、クエリパラメーターとしてURLの後ろに付ける。クエリパラメーターとは、URLの `?` より後ろに付ける検索条件などの情報を指す。

例：

```text
/todos?keyword=課題&category=プログラミング&order=asc
```

これにより、条件を含むURLをそのまま開き直せるようにする。

## 8. メッセージと例外時の動作

### 8.1 操作結果のメッセージ

一覧画面の最上部に、直前の操作結果を次のとおり表示する。

| 要件ID | 条件 | 表示するメッセージ |
|---|---|---|
| MS-01 | 登録が完了した | 登録しました |
| MS-02 | 編集内容の保存が完了した | 保存しました |
| MS-03 | 削除が完了した | 削除しました |
| MS-04 | 指定されたIDが存在しない | 見つかりませんでした |

### 8.2 存在しないIDを指定した場合

編集画面または削除確認画面のURLに存在しないIDが指定された場合は、専用のエラー画面を表示せず一覧画面へ戻す。一覧画面の最上部には「見つかりませんでした」と表示する。

## 9. ログ要件

ログとは、アプリ内で行われた処理の記録を指す。

登録・編集・削除が実行されたときは、操作ごとに1行のログを残す。

ログには次の情報を含める。

- 操作の種類（登録・編集・削除）
- 対象データのID

ログには、やることの内容である `title` と、メモの内容である `detail` を含めない。

## 10. 画面表示の共通要件

画面の詳しい配置は別途作成する画面設計書で扱う。本書では、すべての画面で守る共通条件を定める。

### 10.1 CSS

CSSとは、画面の色・余白・文字サイズなどの見た目を指定する仕組みを指す。

- やることを管理する6画面では、`src/main/resources/static/css/style.css` の1ファイルだけを共通で使用する。
- 各テンプレートの `<head>` から、`<link rel="stylesheet" href="/css/style.css">` の1行で読み込む。
- HTML内に `style="..."` を直接記述しない。
- 2枚目のCSSファイルを作らない。
- 影、動き、画像、外部から読み込むCSSは使用しない。
- CSSの具体的な内容は、`handout_app_spec.md` の「6-3. CSS」に記載された内容から変更しない。

### 10.2 使用するclass名

class名とは、同じ見た目を適用するHTML要素に付ける名前を指す。次の名前だけを使用し、新しいclass名は追加しない。

| 用途 | class名 | 対象画面 |
|---|---|---|
| 画面全体の入れ物 | `container` | 6画面すべて |
| 画面のタイトル | `page-title` | 6画面すべて |
| 一覧の表 | `todo-table` | 一覧 |
| 直前の操作結果 | `flash` | 一覧 |
| 絞り込みと並び替えの領域 | `search-bar` | 一覧 |
| 入力欄1行 | `form-row` | 登録・編集 |
| ラベル | `form-label` | 登録・編集 |
| 入力欄・選択欄 | `form-input` | 登録・編集 |
| 入力エラーの文 | `error` | 登録・編集 |
| 確認画面の値 | `confirm-value` | 登録確認・編集確認・削除確認 |
| 確認画面のボタン領域 | `confirm-actions` | 登録確認・編集確認・削除確認 |
| 主なボタン | `btn` | 6画面すべて |
| 補助ボタン | `btn sub` | 6画面すべて |
| 文字のリンク | `link` | 一覧 |

## 11. 対象外の機能

次の機能は今回の開発対象に含めない。

- 一覧のページ分け
- 削除したデータを元に戻す機能
- 完了にした日時の記録
- ログイン機能
- 複数人での共有
- 写真やファイルの添付

`handout_app_spec.md` に記載されていない機能も、今回の開発対象には追加しない。

## 12. 叩き台から補足した点

新しい機能や動作は追加していない。文書として管理しやすくするため、次の編集上の情報を追加した。

- 要件を見分けるための要件ID（`FR`・`SC`・`DT`・`VL`・`MS`）
- 内容を探しやすくするための見出しと分類
- 初めて読む人のための用語の短い説明

また、次の点は `handout_app_spec.md` に具体的な決まりがないため、本書でも動作を決めていない。

- 期限が空欄のデータを並び替えたときに、先頭と末尾のどちらへ置くか
- 同じ期限のデータが複数ある場合の並び順
- 名前検索での英字の大文字・小文字や、文字幅の違いの扱い
- 不正な検索条件や並び順がURLに指定された場合の扱い
- ログの出力先、書式、出力レベル

これらを決める場合は、要件の追加または変更として記録する。
