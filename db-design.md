# やること管理アプリ DB設計書

## 1. 本書の目的

本書は、やること管理アプリで使用するデータベースの構造を定めるものである。

DBはデータベースの略で、アプリが使用するデータを保存する仕組みを指す。本書は `requirements.md` と `handout_app_spec.md` をもとに作成している。

画面の配置や表示方法は本書の対象外とし、別途作成する画面設計書で定める。

## 2. 使用するデータベース

| 項目 | 設計内容 |
|---|---|
| データベース製品 | MySQL |
| 使用するテーブル数 | 1 |
| テーブル名 | `todos` |
| 1行が表すもの | 1件のやること |

テーブルとは、同じ種類のデータを行と列に分けて保存する入れ物を指す。

## 3. テーブル構成

### 3.1 テーブル概要

| 論理名 | 物理名 | 用途 |
|---|---|---|
| やること | `todos` | やることの内容、分類、期限、状態、登録・更新日時を保存する |

論理名とは人が内容を理解するための日本語名、物理名とはプログラムやSQLで使用する名前を指す。

### 3.2 カラム定義

カラムとは、テーブルの縦方向の項目を指す。NULLは、値が入っていない状態を表す。

| # | 論理名 | カラム名 | データ型 | NULL | 既定値 | キー・自動設定 | 説明 |
|---|---|---|---|---|---|---|---|
| 1 | ID | `id` | `BIGINT` | 不可 | なし | 主キー・`AUTO_INCREMENT` | 1件を見分ける番号。MySQLが1ずつ増やして付ける |
| 2 | やること | `title` | `VARCHAR(255)` | 不可 | なし | なし | やることの内容。255文字以内 |
| 3 | メモ | `detail` | `VARCHAR(255)` | 可 | `NULL` | なし | 任意のメモ。入力する場合は255文字以内 |
| 4 | ジャンル | `category` | `VARCHAR(255)` | 不可 | なし | なし | 決められた5種類のいずれか |
| 5 | 優先度 | `priority` | `INT` | 不可 | `2` | なし | `1`は高、`2`は中、`3`は低 |
| 6 | 期限 | `due_date` | `DATE` | 可 | `NULL` | なし | 任意の期限日 |
| 7 | 完了状態 | `completed` | `BOOLEAN` | 不可 | `FALSE` | なし | `TRUE`は完了、`FALSE`は未完了 |
| 8 | 登録日時 | `created_at` | `DATETIME` | 不可 | `CURRENT_TIMESTAMP` | 登録時に自動設定 | そのデータを登録した日時 |
| 9 | 更新日時 | `updated_at` | `DATETIME` | 不可 | `CURRENT_TIMESTAMP` | 登録時および更新時に自動設定 | そのデータを最後に変更した日時 |

主キーとは、テーブルの各行を重複なく見分けるためのカラムを指す。`AUTO_INCREMENT` は、新しい行を追加するたびに番号を自動で1ずつ増やすMySQLの機能である。

### 3.3 データ型の説明

データ型とは、そのカラムに保存できる値の種類を表す。

| データ型 | 意味 |
|---|---|
| `BIGINT` | 大きな整数を保存する型 |
| `VARCHAR(255)` | 最大255文字の文字列を保存する型 |
| `INT` | 整数を保存する型 |
| `DATE` | 年月日を保存する型 |
| `BOOLEAN` | 真偽値を保存する型。MySQLでは通常、`TRUE`を`1`、`FALSE`を`0`として扱う |
| `DATETIME` | 年月日と時刻を保存する型 |

## 4. キーと制約

制約とは、不正なデータが保存されにくいようにデータベースへ設定する決まりを指す。

### 4.1 主キー

- `id` を主キーとする。
- `id` は重複を許可しない。
- `id` はNULLを許可しない。
- `id` は `AUTO_INCREMENT` によりMySQLが自動で付ける。

### 4.2 NULL制約

次のカラムは必須とし、NULLを許可しない。

- `id`
- `title`
- `category`
- `priority`
- `completed`
- `created_at`
- `updated_at`

次のカラムは任意とし、NULLを許可する。

- `detail`
- `due_date`

`NOT NULL` はNULLを許可しない指定、`NULL` は値がなくてもよい指定である。

### 4.3 既定値と自動設定

既定値とは、登録処理で値を指定しなかった場合に自動で使用される値を指す。

| カラム | 設計内容 |
|---|---|
| `priority` | 値を指定しなかった場合は `2`（中）とする |
| `completed` | 値を指定しなかった場合は `FALSE`（未完了）とする |
| `created_at` | 登録時の日時を自動で設定する |
| `updated_at` | 登録時の日時を設定し、その後はデータの更新時に自動で更新する |

登録画面では優先度を必ず選ばせるため、`priority` の既定値は、アプリを通さず値が省略された場合にも備えるデータベース側の設定として扱う。

### 4.4 外部キー

外部キーとは、別のテーブルのデータとの関係を保つための設定を指す。本アプリで使用するテーブルは `todos` だけなので、外部キーは設定しない。

### 4.5 インデックス

インデックスとは、データを探したり並べ替えたりする処理を速くするための索引を指す。

本設計では、主キー `id` にMySQLが作成するインデックスだけを使用し、追加のインデックスは作成しない。要件にはデータ件数や処理速度の目標が定められていないためである。

## 5. 保存する値の決まり

### 5.1 ジャンル

`category` には、次のいずれかの文字列を保存する。

| 保存する値 |
|---|
| デザイン |
| マーケティング |
| プログラミング |
| 資格 |
| 就職活動 |

自由入力は許可せず、アプリの入力チェックで5種類以外の値を保存しないようにする。

### 5.2 優先度

| 保存する値 | 意味 | 画面に表示する言葉 |
|---|---|---|
| `1` | 高い優先度 | 高 |
| `2` | 中程度の優先度 | 中 |
| `3` | 低い優先度 | 低 |

アプリの入力チェックで、`1`・`2`・`3` 以外の値を保存しないようにする。

### 5.3 完了状態

| 保存する値 | 意味 | 画面に表示する言葉 |
|---|---|---|
| `FALSE`（`0`） | 未完了 | 未完了 |
| `TRUE`（`1`） | 完了 | 完了 |

新規登録時は `FALSE`（未完了）を使用する。完了状態は編集時に変更できるようにする。

### 5.4 空欄の扱い

- `detail` が入力されていない場合は `NULL` を保存する。
- `due_date` が入力されていない場合は `NULL` を保存する。
- 必須項目に半角空白または全角空白だけが入力された場合は、アプリの入力チェックで保存を止める。
- `NOT NULL` だけでは空文字や空白だけの文字列を防げないため、`title`・`category` の内容はアプリ側でも確認する。

## 6. データ操作の設計

### 6.1 登録

新しいやることを登録するときは、次のカラムへアプリから値を渡す。

- `title`
- `detail`
- `category`
- `priority`
- `due_date`

次のカラムはMySQLが自動で設定するため、利用者には入力させない。

- `id`
- `completed`
- `created_at`
- `updated_at`

`completed` は既定値の `FALSE` を使用する。

### 6.2 一覧・検索・並び替え

- 一覧表示では、`todos` のデータを取得する。
- 名前による部分一致検索では `title` を使用する。
- ジャンルによる完全一致検索では `category` を使用する。
- 期限の並び替えでは `due_date` を使用する。
- 一覧の編集・削除対象を指定するときは `id` を使用する。

部分一致とは、入力した文字が対象の文字列の一部に含まれている状態を指す。完全一致とは、2つの値がすべて同じである状態を指す。

### 6.3 編集

編集時は `id` で対象を特定し、次のカラムを更新対象とする。

- `title`
- `detail`
- `category`
- `priority`
- `due_date`
- `completed`

更新が行われると、`updated_at` はMySQLによって現在日時へ自動更新される。`id` と `created_at` は変更しない。

### 6.4 削除

削除時は `id` で対象を特定し、該当する1行を削除する。削除した行を別のテーブルへ残す処理や、削除後に元へ戻す処理は行わない。

## 7. CREATE TABLE文

DDLとは、テーブルなどのデータベース構造を作成・変更するSQL文を指す。次のDDLで `todos` テーブルを作成する。

```sql
CREATE TABLE todos (
    id BIGINT NOT NULL AUTO_INCREMENT,
    title VARCHAR(255) NOT NULL,
    detail VARCHAR(255) NULL,
    category VARCHAR(255) NOT NULL,
    priority INT NOT NULL DEFAULT 2,
    due_date DATE NULL,
    completed BOOLEAN NOT NULL DEFAULT FALSE,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id)
);
```

このDDLは設計内容を示すものであり、本書の作成時には実行しない。

`category` と `priority` の値の種類を制限する `CHECK` 制約は、このDDLには追加しない。`handout_app_spec.md` に `CHECK` 制約の指定がなく、使用するMySQLのバージョンも定められていないためである。許可する値は、アプリの入力チェックで制限する。

## 8. 要件とカラムの対応

トレーサビリティとは、要件と設計内容のつながりを後から確認できるようにすることを指す。次の表で、`requirements.md` の要件と各カラムの対応を示す。

| カラム | 対応する要件 | 対応内容 |
|---|---|---|
| `id` | `DT-01`、`FR-03`、`FR-04`、`MS-04`、9章 | 1件の識別、編集・削除対象の指定、存在確認、操作ログに使用する |
| `title` | `DT-02`、`FR-01`、`FR-02`、`FR-03`、`FR-05`、`VL-01`、`VL-02` | 一覧表示、登録・編集、名前検索、必須・文字数確認に使用する |
| `detail` | `DT-03`、`FR-02`、`FR-03`、`VL-03` | 任意のメモとして登録・編集し、文字数確認を行う |
| `category` | `DT-04`、`FR-01`、`FR-02`、`FR-03`、`FR-05`、`VL-04` | 一覧表示、登録・編集、ジャンル検索、必須確認に使用する |
| `priority` | `DT-05`、`FR-01`、`FR-02`、`FR-03`、`VL-05` | 優先度を登録・編集し、一覧用の表示へ変換する |
| `due_date` | `DT-06`、`FR-01`、`FR-02`、`FR-03`、`FR-06` | 任意の期限を登録・編集し、一覧表示と並び替えに使用する |
| `completed` | `DT-07`、`FR-01`、`FR-03` | 未完了・完了の状態を保存し、一覧表示と編集に使用する |
| `created_at` | `DT-08` | 登録日時をMySQLが自動で記録する |
| `updated_at` | `DT-09` | 最後に更新した日時をMySQLが自動で記録する |

### 8.1 `handout_app_spec.md` との対応

| 設計内容 | 対応する箇所 |
|---|---|
| テーブル名、9カラム、型、NULL、既定値、自動設定 | 2. 保存するもの |
| ジャンルの保存値 | 2. ジャンルの5つ |
| 利用者が入力しないカラム | 2. 自分では入力しない列 |
| 優先度と完了状態の値 | 4. 一覧 |
| 文字数・必須項目・空白の確認 | 5-1. 入力のきまり |
| `id` による存在確認 | 5-2. 存在しない番号を開いたとき |
| `title`・`category` の検索と `due_date` の並び替え | 5-4. 検索と並び替え |
| ログに残す `id` | 5-5. 記録（ログ）に残すもの |

## 9. 本設計で追加した判断と未決定事項

### 9.1 本設計で追加した判断

元の2文書にない機能やカラムは追加していない。DB設計書として実行可能なDDLにするため、次の設計上の判断を明示した。

- 任意項目の `detail` と `due_date` は、未入力時に `NULL` を保存する。
- 主キー以外の追加インデックスは作成しない。
- `category` と `priority` の値の種類は、DBの `CHECK` 制約ではなくアプリの入力チェックで制限する。
- `updated_at` は、`ON UPDATE CURRENT_TIMESTAMP` を使って更新時に自動更新する。

### 9.2 未決定事項

次の項目は元の2文書で決まっていないため、本設計でも値を決めていない。

- データベース名
- 使用するMySQLのバージョン
- 文字コードと照合順序
- `due_date` がNULLの場合の並び順
- 同じ期限を持つデータ同士の並び順
- 名前検索における英字の大文字・小文字や文字幅の扱い
- ログの保存先、書式、出力レベル

照合順序とは、文字列を比較・並び替えするときの規則を指す。文字コードと照合順序は、使用するデータベースの設定を確認したうえで決定する。
