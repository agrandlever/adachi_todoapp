# やること管理アプリ 画面設計書

## 1. 本書の目的

本書は、やること管理アプリで新しく作る6画面について、画面に表示する内容、利用者が操作できるもの、入力エラーの文、画面から画面へ移る流れを定めるものである。

本書は、次の3ファイルをもとに作成している。

- `handout_app_spec.md`
- `requirements.md`
- `db-design.md`

画面遷移とは、ボタンやリンクを押したときに、どの画面へ移るかという流れを指す。

## 2. 設計対象と共通ルール

### 2.1 設計する画面

設計対象は、次の6画面だけとする。

| 画面ID | 画面名     | URL                   | 目的                                         |
| ------ | ---------- | --------------------- | -------------------------------------------- |
| SC-02  | 一覧       | `/todos`              | 登録したやることを表示し、各操作の入口とする |
| SC-03  | 登録       | `/todos/new`          | 新しいやることを入力する                     |
| SC-04  | 登録の確認 | `/todos/confirm`      | 登録前に入力内容を確認する                   |
| SC-05  | 編集       | `/todos/{id}/edit`    | 登録済みのやることを変更する                 |
| SC-06  | 編集の確認 | `/todos/{id}/confirm` | 保存前に変更内容を確認する                   |
| SC-07  | 削除確認   | `/todos/{id}/delete`  | 削除前に対象を確認する                       |

`{id}` には、対象となる1件を見分ける番号が入る。

既存のトップページ `/` はそのまま使用し、本書で設計する6画面には含めない。専用のエラー画面も追加しない。

### 2.2 共通の見た目

CSSとは、画面の色、余白、文字サイズなどの見た目を指定する仕組みを指す。

- 6画面すべてで `src/main/resources/static/css/style.css` の1ファイルだけを使用する。
- 各画面の `<head>` で `<link rel="stylesheet" href="/css/style.css">` を読み込む。
- HTML内に `style="..."` を直接記述しない。
- 2枚目のCSS、影、動き、画像、外部から読み込むCSSは使用しない。
- 画面全体を `container` の中に置き、画面名を `page-title` で表示する。
- `handout_app_spec.md` に定められたCSSの内容を変更しない。

class名とは、同じ見た目を適用する場所に付ける名前を指す。使用できるclass名は次のものだけとし、新しい名前は追加しない。

| 用途                     | class名           | 使用する画面                                 |
| ------------------------ | ----------------- | -------------------------------------------- |
| 画面全体の入れ物         | `container`       | 6画面すべて                                  |
| 画面のタイトル           | `page-title`      | 6画面すべて                                  |
| 一覧の表                 | `todo-table`      | 一覧                                         |
| 直前の操作結果           | `flash`           | 一覧                                         |
| 絞り込みと並び替えの領域 | `search-bar`      | 一覧                                         |
| 入力欄1行                | `form-row`        | 登録、編集                                   |
| ラベル                   | `form-label`      | 登録、編集                                   |
| 入力欄、選択欄           | `form-input`      | 登録、編集                                   |
| 入力エラーの文           | `error`           | 登録、編集                                   |
| 確認画面の値             | `confirm-value`   | 登録の確認、編集の確認、削除確認             |
| 確認画面のボタン領域     | `confirm-actions` | 登録の確認、編集の確認、削除確認             |
| 主なボタン               | `btn`             | 6画面すべて                                  |
| 補助ボタン               | `btn sub`         | 登録、登録の確認、編集、編集の確認、削除確認 |
| 文字のリンク             | `link`            | 一覧                                         |

`btn sub` は、`btn` と `sub` の2つのclass名を同じ要素に付ける指定である。

### 2.3 共通の表示ルール

#### ジャンル

ジャンルは自由入力にせず、次の5つから選ぶ。

- デザイン
- マーケティング
- プログラミング
- 資格
- 就職活動

#### 優先度と状態

データベースに保存されている値は、画面では次の言葉に変えて表示する。

| 項目     | 保存されている値   | 画面に表示する言葉 |
| -------- | ------------------ | ------------------ |
| 優先度   | `1`                | 高                 |
| 優先度   | `2`                | 中                 |
| 優先度   | `3`                | 低                 |
| 完了状態 | `FALSE` または `0` | 未完了             |
| 完了状態 | `TRUE` または `1`  | 完了               |

#### 入力されていない任意項目

- 登録の確認画面と編集の確認画面では、メモまたは期限が未入力の場合に「（入力なし）」と表示する。
- 削除確認画面では、期限が未入力の場合に「（入力なし）」と表示する。
- 一覧画面では、期限が未入力の場合は期限のセルを空欄にする。

#### 画面に表示しない項目

次の項目は利用者が入力する欄を作らず、一覧や確認内容にも表示しない。

- ID（`id`）
- 登録日時（`created_at`）
- 更新日時（`updated_at`）

IDは、編集と削除の対象を特定するため、画面のURLの `{id}` にだけ使用する。

## 3. 画面遷移

### 3.1 全体の流れ

```text
一覧 ──「新規登録」──→ 登録 ──「確認する」──→ 登録の確認 ──「登録する」──→ 一覧
                          ↑                        │
                          └────「書き直す」────┘

一覧 ──行の「編集」──→ 編集 ──「確認する」──→ 編集の確認 ──「保存する」──→ 一覧
                          ↑                        │
                          └────「書き直す」────┘

一覧 ──行の「削除」──→ 削除確認 ──「消す」──→ 一覧
                            └──「やめる」──→ 一覧
```

登録画面と編集画面の「一覧へ」も、一覧画面へ戻る。

一覧画面の「絞り込む」「期限が近い順」「期限が遠い順」は、別画面へ移らず、条件を反映した一覧画面を再表示する。

### 3.2 操作ごとの遷移

| 現在の画面 | 画面に出す操作名 | 移動先     | 移動時の動作                                     |
| ---------- | ---------------- | ---------- | ------------------------------------------------ |
| 一覧       | 絞り込む         | 一覧       | 名前とジャンルの条件で一覧を再表示する           |
| 一覧       | 期限が近い順     | 一覧       | 期限が近い順で一覧を再表示する                   |
| 一覧       | 期限が遠い順     | 一覧       | 期限が遠い順で一覧を再表示する                   |
| 一覧       | 新規登録         | 登録       | 空の入力欄を表示する                             |
| 一覧       | 編集             | 編集       | 選んだ1件の現在値を表示する                      |
| 一覧       | 削除             | 削除確認   | 選んだ1件の削除確認内容を表示する                |
| 登録       | 確認する         | 登録の確認 | 入力にエラーがなければ、入力内容を文字で表示する |
| 登録       | 一覧へ           | 一覧       | 登録せずに一覧へ戻る                             |
| 登録の確認 | 登録する         | 一覧       | 1件を登録し、「登録しました」を表示する          |
| 登録の確認 | 書き直す         | 登録       | 入力済みの値を保って戻る                         |
| 編集       | 確認する         | 編集の確認 | 入力にエラーがなければ、変更内容を文字で表示する |
| 編集       | 一覧へ           | 一覧       | 保存せずに一覧へ戻る                             |
| 編集の確認 | 保存する         | 一覧       | 変更を保存し、「保存しました」を表示する         |
| 編集の確認 | 書き直す         | 編集       | 変更後の値を保って戻る                           |
| 削除確認   | 消す             | 一覧       | 対象を削除し、「削除しました」を表示する         |
| 削除確認   | やめる           | 一覧       | 削除せずに一覧へ戻る                             |

### 3.3 入力エラーがある場合の動き

「確認する」を押したときに入力エラーがある場合は、確認画面へ移らない。登録または編集の入力画面を再表示し、該当する欄のすぐ下にエラーの文を表示する。

半角空白または全角空白だけの「やること」は、空欄として扱う。

### 3.4 存在しないIDを指定した場合の動き

編集画面 `/todos/{id}/edit` または削除確認画面 `/todos/{id}/delete` に、データベースに存在しないIDを指定した場合は一覧画面へ移す。専用画面は作らず、一覧の最上部に「見つかりませんでした」と表示する。

## 4. 画面別設計

### 4.1 一覧（SC-02）

#### 基本情報

| 項目         | 設計内容                                                                     |
| ------------ | ---------------------------------------------------------------------------- |
| URL          | `/todos`                                                                     |
| 画面の見出し | 一覧                                                                         |
| 役割         | 登録済みのやることを表示し、絞り込み、並び替え、登録、編集、削除の入口とする |

#### 配置

上から下へ、次の順に配置する。

```text
┌────────────────────────────────────────────┐
│ 操作結果の文（表示する必要があるときだけ） │
│ 見出し「一覧」                              │
│                                            │
│ やることの名前 [入力欄]                    │
│ ジャンル [すべて ▼] [絞り込む]             │
│ 並び替え 期限が近い順／期限が遠い順         │
│ [新規登録]                                  │
│                                            │
│ やること│ジャンル│優先度│期限│状態│操作     │
│ （一覧の各行）                 編集／削除    │
└────────────────────────────────────────────┘
```

操作結果の文は、直前の操作結果がある場合だけ表示する。

#### 絞り込みと並び替えの領域

| 種類          | 画面に出す言葉 | 初期状態または選択肢                                             | 動作                                                                 |
| ------------- | -------------- | ---------------------------------------------------------------- | -------------------------------------------------------------------- |
| 1行の文字入力 | やることの名前 | 絞り込みなしの場合は空欄                                         | 入力した言葉が「やること」の一部に含まれる行を残す                   |
| 選択欄        | ジャンル       | すべて、デザイン、マーケティング、プログラミング、資格、就職活動 | 選んだジャンルと同じ行だけを残す。「すべて」はジャンルで絞り込まない |
| ボタン        | 絞り込む       | `btn`                                                            | 入力した条件をURLに付けて一覧を再表示する                            |
| 文字のリンク  | 期限が近い順   | `link`                                                           | `due_date` の近い順で一覧を再表示する                                |
| 文字のリンク  | 期限が遠い順   | `link`                                                           | `due_date` の遠い順で一覧を再表示する                                |
| ボタン        | 新規登録       | `btn`                                                            | 登録画面へ移る                                                       |

一覧を最初に開いたときは「期限が近い順」とする。現在の絞り込み条件と並び順は、クエリパラメーターとしてURLの `?` より後ろに付ける。

```text
/todos?keyword=課題&category=プログラミング&order=asc
```

上の例では、`keyword` が名前、`category` がジャンル、`order` が並び順を表す。`asc` は期限が近い順、`desc` は期限が遠い順として扱う。

#### 一覧表

表の列は、左から次の順にする。

| 列の見出し | 表示内容                                     |
| ---------- | -------------------------------------------- |
| やること   | `title` の文字                               |
| ジャンル   | `category` の文字                            |
| 優先度     | `priority` を「高」「中」「低」に変えた言葉  |
| 期限       | `due_date`。未入力の場合は空欄               |
| 状態       | `completed` を「完了」「未完了」に変えた言葉 |
| 操作       | 行ごとの「編集」と「削除」                   |

一覧表には `todo-table` を使用し、「編集」と「削除」には `link` を使用する。

表示するデータが0件の場合は、未登録の場合と絞り込み結果が0件の場合のどちらでも「まだ1件も登録されていません」と表示する。0件専用の別の文は追加しない。

#### 操作結果の文

一覧の最上部に、直前の結果を `flash` で1行表示する。

| 条件                     | 表示する文           |
| ------------------------ | -------------------- |
| 登録が完了した           | 登録しました         |
| 編集内容の保存が完了した | 保存しました         |
| 削除が完了した           | 削除しました         |
| 指定したIDが存在しない   | 見つかりませんでした |

### 4.2 登録（SC-03）

#### 基本情報

| 項目         | 設計内容                    |
| ------------ | --------------------------- |
| URL          | `/todos/new`                |
| 画面の見出し | 登録                        |
| 役割         | 新しいやることを1件入力する |

#### 配置

```text
┌──────────────────────────────┐
│ 見出し「登録」                │
│ やること [1行の文字入力]      │
│   エラーの文                  │
│ メモ     [1行の文字入力]      │
│   エラーの文                  │
│ ジャンル [空欄 ▼]             │
│   エラーの文                  │
│ 優先度   [空欄 ▼]             │
│   エラーの文                  │
│ 期限     [日付入力]           │
│ [確認する] [一覧へ]           │
└──────────────────────────────┘
```

エラーの文は、該当するエラーがあるときだけ表示する。

#### 入力項目

| 順番 | ラベル   | 必須か | 入力方法      | 初期状態           | 選択肢                                                   |
| ---- | -------- | ------ | ------------- | ------------------ | -------------------------------------------------------- |
| 1    | やること | 必須   | 1行の文字入力 | 空欄               | なし                                                     |
| 2    | メモ     | 任意   | 1行の文字入力 | 空欄               | なし                                                     |
| 3    | ジャンル | 必須   | 選択欄        | 表示文字のない空欄 | デザイン、マーケティング、プログラミング、資格、就職活動 |
| 4    | 優先度   | 必須   | 選択欄        | 表示文字のない空欄 | 高、中、低                                               |
| 5    | 期限     | 任意   | 日付入力      | 空欄               | なし                                                     |

各項目を `form-row` に入れ、ラベルに `form-label`、入力欄または選択欄に `form-input` を使用する。エラーの文には `error` を使用し、該当する欄のすぐ下に表示する。

#### ボタンと移動先

| ボタン名 | class名   | 移動先     | 動作                                         |
| -------- | --------- | ---------- | -------------------------------------------- |
| 確認する | `btn`     | 登録の確認 | 入力を確認し、エラーがなければ入力内容を渡す |
| 一覧へ   | `btn sub` | 一覧       | 登録せずに一覧へ戻る                         |

### 4.3 登録の確認（SC-04）

#### 基本情報

| 項目         | 設計内容                         |
| ------------ | -------------------------------- |
| URL          | `/todos/confirm`                 |
| 画面の見出し | 登録の確認                       |
| 役割         | 入力された内容を登録前に確認する |

#### 配置

```text
┌──────────────────────────┐
│ 見出し「登録の確認」      │
│ やること                  │
│   入力された値            │
│ メモ                      │
│   入力された値            │
│ ジャンル                  │
│   選ばれた値              │
│ 優先度                    │
│   高／中／低              │
│ 期限                      │
│   入力された値            │
│ [登録する] [書き直す]     │
└──────────────────────────┘
```

入力欄は表示せず、値を文字として表示する。各値に `confirm-value`、ボタンを並べる領域に `confirm-actions` を使用する。

#### 確認項目

| 順番 | ラベル   | 表示内容                                       |
| ---- | -------- | ---------------------------------------------- |
| 1    | やること | 入力された文字                                 |
| 2    | メモ     | 入力された文字。未入力の場合は「（入力なし）」 |
| 3    | ジャンル | 選ばれたジャンル                               |
| 4    | 優先度   | 高、中、低のいずれか                           |
| 5    | 期限     | 入力された日付。未入力の場合は「（入力なし）」 |

#### ボタンと移動先

| ボタン名 | class名   | 移動先 | 動作                                          |
| -------- | --------- | ------ | --------------------------------------------- |
| 登録する | `btn`     | 一覧   | 1件を登録し、一覧に「登録しました」を表示する |
| 書き直す | `btn sub` | 登録   | 入力済みの5項目を保ったまま戻る               |

### 4.4 編集（SC-05）

#### 基本情報

| 項目         | 設計内容                |
| ------------ | ----------------------- |
| URL          | `/todos/{id}/edit`      |
| 画面の見出し | 編集                    |
| 役割         | 登録済みの1件を変更する |

#### 配置

```text
┌──────────────────────────────┐
│ 見出し「編集」                │
│ やること [現在の値]           │
│   エラーの文                  │
│ メモ     [現在の値]           │
│   エラーの文                  │
│ ジャンル [現在の値 ▼]         │
│   エラーの文                  │
│ 優先度   [現在の値 ▼]         │
│   エラーの文                  │
│ 期限     [現在の日付]         │
│ 完了     [チェック欄]         │
│ [確認する] [一覧へ]           │
└──────────────────────────────┘
```

エラーの文は、該当するエラーがあるときだけ表示する。

#### 入力項目

| 順番 | ラベル   | 必須か         | 入力方法      | 開いたときの表示                             | 選択肢                                                   |
| ---- | -------- | -------------- | ------------- | -------------------------------------------- | -------------------------------------------------------- |
| 1    | やること | 必須           | 1行の文字入力 | 保存中の値                                   | なし                                                     |
| 2    | メモ     | 任意           | 1行の文字入力 | 保存中の値。未入力なら空欄                   | なし                                                     |
| 3    | ジャンル | 必須           | 選択欄        | 保存中の値を選択                             | デザイン、マーケティング、プログラミング、資格、就職活動 |
| 4    | 優先度   | 必須           | 選択欄        | 保存中の値を「高」「中」「低」に変えて選択   | 高、中、低                                               |
| 5    | 期限     | 任意           | 日付入力      | 保存中の日付。未入力なら空欄                 | なし                                                     |
| 6    | 完了     | チェックは任意 | チェック欄    | 完了ならチェックあり、未完了ならチェックなし | チェックあり／なし                                       |

各項目を `form-row` に入れ、ラベルに `form-label` を使用する。文字入力、選択欄、日付入力には `form-input` を使用する。エラーの文には `error` を使用し、該当する欄のすぐ下に表示する。

#### ボタンと移動先

| ボタン名 | class名   | 移動先     | 動作                                         |
| -------- | --------- | ---------- | -------------------------------------------- |
| 確認する | `btn`     | 編集の確認 | 入力を確認し、エラーがなければ変更内容を渡す |
| 一覧へ   | `btn sub` | 一覧       | 保存せずに一覧へ戻る                         |

指定されたIDが存在しない場合は一覧へ移り、「見つかりませんでした」と表示する。

### 4.5 編集の確認（SC-06）

#### 基本情報

| 項目         | 設計内容                   |
| ------------ | -------------------------- |
| URL          | `/todos/{id}/confirm`      |
| 画面の見出し | 編集の確認                 |
| 役割         | 変更内容を保存前に確認する |

#### 配置

```text
┌──────────────────────────┐
│ 見出し「編集の確認」      │
│ やること                  │
│   変更後の値              │
│ メモ                      │
│   変更後の値              │
│ ジャンル                  │
│   変更後の値              │
│ 優先度                    │
│   高／中／低              │
│ 期限                      │
│   変更後の値              │
│ 完了                      │
│   完了／未完了            │
│ [保存する] [書き直す]     │
└──────────────────────────┘
```

入力欄は表示せず、値を文字として表示する。各値に `confirm-value`、ボタンを並べる領域に `confirm-actions` を使用する。

#### 確認項目

| 順番 | ラベル   | 表示内容                                     |
| ---- | -------- | -------------------------------------------- |
| 1    | やること | 変更後の文字                                 |
| 2    | メモ     | 変更後の文字。未入力の場合は「（入力なし）」 |
| 3    | ジャンル | 変更後のジャンル                             |
| 4    | 優先度   | 高、中、低のいずれか                         |
| 5    | 期限     | 変更後の日付。未入力の場合は「（入力なし）」 |
| 6    | 完了     | 完了または未完了                             |

#### ボタンと移動先

| ボタン名 | class名   | 移動先 | 動作                                           |
| -------- | --------- | ------ | ---------------------------------------------- |
| 保存する | `btn`     | 一覧   | 変更を保存し、一覧に「保存しました」を表示する |
| 書き直す | `btn sub` | 編集   | 変更後の6項目を保ったまま戻る                  |

### 4.6 削除確認（SC-07）

#### 基本情報

| 項目         | 設計内容                        |
| ------------ | ------------------------------- |
| URL          | `/todos/{id}/delete`            |
| 画面の見出し | 削除確認                        |
| 役割         | 削除する1件を、削除前に確認する |

#### 配置

```text
┌──────────────────────────┐
│ 見出し「削除確認」        │
│ やること                  │
│   削除する1件の値         │
│ ジャンル                  │
│   削除する1件の値         │
│ 期限                      │
│   削除する1件の値         │
│ [消す] [やめる]           │
└──────────────────────────┘
```

入力欄は表示せず、値を文字として表示する。各値に `confirm-value`、ボタンを並べる領域に `confirm-actions` を使用する。

#### 確認項目

| 順番 | ラベル   | 表示内容                                                 |
| ---- | -------- | -------------------------------------------------------- |
| 1    | やること | 削除する1件の `title`                                    |
| 2    | ジャンル | 削除する1件の `category`                                 |
| 3    | 期限     | 削除する1件の `due_date`。未入力の場合は「（入力なし）」 |

#### ボタンと移動先

| ボタン名 | class名   | 移動先 | 動作                                           |
| -------- | --------- | ------ | ---------------------------------------------- |
| 消す     | `btn`     | 一覧   | 対象を削除し、一覧に「削除しました」を表示する |
| やめる   | `btn sub` | 一覧   | 削除せずに一覧へ戻る                           |

指定されたIDが存在しない場合は一覧へ移り、「見つかりませんでした」と表示する。

## 5. 入力チェックとエラー表示

入力チェックとは、入力内容が決まりを守っているか確認する処理を指す。登録画面と編集画面に同じ決まりを適用する。

| 対象     | エラーとなる条件                       | 画面に出す文                            | 表示位置           |
| -------- | -------------------------------------- | --------------------------------------- | ------------------ |
| やること | 空欄、半角空白だけ、または全角空白だけ | やることを入力してください              | やること欄のすぐ下 |
| やること | 255文字を超えている                    | やることは255文字以内で入力してください | やること欄のすぐ下 |
| メモ     | 255文字を超えている                    | メモは255文字以内で入力してください     | メモ欄のすぐ下     |
| ジャンル | 選択されていない                       | ジャンルを選んでください                | ジャンル欄のすぐ下 |
| 優先度   | 選択されていない                       | 優先度を選んでください                  | 優先度欄のすぐ下   |

エラーが1つ以上ある場合は保存せず、確認画面にも移らない。期限には追加の入力チェックを設けない。

## 6. 画面に出す言葉の一覧

この章は、実装時に表記の揺れを防ぐための確認用一覧である。

### 6.1 画面の見出し

| 画面       | 見出しとして出す言葉 |
| ---------- | -------------------- |
| 一覧       | 一覧                 |
| 登録       | 登録                 |
| 登録の確認 | 登録の確認           |
| 編集       | 編集                 |
| 編集の確認 | 編集の確認           |
| 削除確認   | 削除確認             |

### 6.2 ボタンとリンク

| 種類           | 画面に出す言葉 |
| -------------- | -------------- |
| ボタン         | 絞り込む       |
| リンク         | 期限が近い順   |
| リンク         | 期限が遠い順   |
| ボタン         | 新規登録       |
| 行ごとのリンク | 編集           |
| 行ごとのリンク | 削除           |
| ボタン         | 確認する       |
| 補助ボタン     | 一覧へ         |
| ボタン         | 登録する       |
| 補助ボタン     | 書き直す       |
| ボタン         | 保存する       |
| ボタン         | 消す           |
| 補助ボタン     | やめる         |

### 6.3 項目名、列名、選択肢

| 分類                       | 画面に出す言葉                                                   |
| -------------------------- | ---------------------------------------------------------------- |
| 入力・確認項目             | やること、メモ、ジャンル、優先度、期限、完了                     |
| 一覧の列                   | やること、ジャンル、優先度、期限、状態、操作                     |
| 絞り込み項目               | やることの名前、ジャンル                                         |
| 並び替えの表示             | 並び替え                                                         |
| ジャンルの選択肢           | すべて、デザイン、マーケティング、プログラミング、資格、就職活動 |
| 優先度の選択肢・表示       | 高、中、低                                                       |
| 完了状態の表示             | 完了、未完了                                                     |
| 確認画面の任意項目が未入力 | （入力なし）                                                     |

登録画面と編集画面のジャンル選択欄には「すべて」を出さない。「すべて」は一覧の絞り込みだけで使用する。

### 6.4 メッセージ

| 分類                  | 画面に出す文                            |
| --------------------- | --------------------------------------- |
| データが0件           | まだ1件も登録されていません             |
| 登録完了              | 登録しました                            |
| 編集保存完了          | 保存しました                            |
| 削除完了              | 削除しました                            |
| IDが存在しない        | 見つかりませんでした                    |
| やることが空欄        | やることを入力してください              |
| やることが255文字超過 | やることは255文字以内で入力してください |
| メモが255文字超過     | メモは255文字以内で入力してください     |
| ジャンルが未選択      | ジャンルを選んでください                |
| 優先度が未選択        | 優先度を選んでください                  |

## 7. 画面項目とデータの対応

| 画面の項目 | データベースのカラム | 使用する画面                                       | 補足                                            |
| ---------- | -------------------- | -------------------------------------------------- | ----------------------------------------------- |
| やること   | `title`              | 一覧、登録、登録の確認、編集、編集の確認、削除確認 | 必須、255文字以内。名前検索にも使用する         |
| メモ       | `detail`             | 登録、登録の確認、編集、編集の確認                 | 任意、255文字以内                               |
| ジャンル   | `category`           | 6画面すべて                                        | 決められた5種類。絞り込みにも使用する           |
| 優先度     | `priority`           | 一覧、登録、登録の確認、編集、編集の確認           | `1`、`2`、`3` を「高」「中」「低」で表示する    |
| 期限       | `due_date`           | 6画面すべて                                        | 任意。並び替えにも使用する                      |
| 完了／状態 | `completed`          | 一覧、編集、編集の確認                             | 新規登録時は未完了。編集で変更できる            |
| ID         | `id`                 | 編集、編集の確認、削除確認への移動                 | 入力欄や確認項目には出さず、URLで対象を特定する |

`created_at` と `updated_at` はMySQLが自動で記録するため、画面項目を作らない。

## 8. 対象外と未決定事項

### 8.1 今回作らないもの

- 7画面目以降の新しい画面
- 一覧のページ分け
- 削除したデータを元に戻す機能
- 完了にした日時の記録
- ログイン機能
- 複数人での共有
- 写真やファイルの添付

### 8.2 元の文書で決まっていないこと

次の内容は、元の3文書で決まっていないため、本書でも新しい動作を決めない。

- 期限が未入力のデータを並び替えたときに、先頭と末尾のどちらへ置くか
- 同じ期限のデータが複数ある場合の並び順
- 名前検索での英字の大文字・小文字や、文字幅の違いの扱い
- 不正な検索条件や並び順がURLに指定された場合の扱い

## 9. 元の文書との対応

| 設計内容                      | 主な参照元                                            |
| ----------------------------- | ----------------------------------------------------- |
| 6画面の名前、URL、目的        | `handout_app_spec.md` 3章、`requirements.md` 3章      |
| 各画面の項目、ボタン、動作    | `handout_app_spec.md` 4章、`requirements.md` 4章      |
| 入力エラーの文                | `handout_app_spec.md` 5-1、`requirements.md` 6章      |
| 存在しないIDと操作結果の文    | `handout_app_spec.md` 5-2・5-3、`requirements.md` 8章 |
| 絞り込み、並び替え、URLの条件 | `handout_app_spec.md` 5-4、`requirements.md` 7章      |
| 共通CSSとclass名              | `handout_app_spec.md` 6章、`requirements.md` 10章     |
| 保存値と画面表示の対応        | `db-design.md` 3章・5章・6章                          |
